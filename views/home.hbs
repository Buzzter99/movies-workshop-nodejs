<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Catalog Page</title>
    <link href="./static/css/style.css" type="text/css" rel="stylesheet">
</head>
<body>
    <div id="box">
        <nav>
            <img src="./static/img/logo.webp" alt="logo">
            <ul class="menu">
                {{> nav}}
            </ul>
        </nav>
        <main>
            <section id="catalog">
                <h1>All Movies</h1>
                <div class="band">
                    {{#each movies}}
                    <div class="flip flip-vertical">
                        <div class="front">
                            <img src="{{imageURL}}"
                                alt="{{imageURL}}">
                        </div>
                        <div class="back">
                            <h1>{{title}}</h1>
                            <h2>Genre: {{genre}}</h2>
                            <p>Description: {{description}}</p>
                            <a href="/details/{{_id}}" class="details">Details</a>
                            <a href="/cast/attach/{{_id}}" class="details">Attach</a>
                        </div>
                    </div>
                    {{else}}
                        <div class="no-posts">
                        <p class="no-offer">There are no posts yet...</p>
                    </div> 
                    {{/each}}
                </div>
            </section>
        </main>
        <footer>
            © JS Back-End Workshop
        </footer>
    </div>
</body>
</html>